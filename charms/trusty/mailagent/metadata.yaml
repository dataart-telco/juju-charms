name: mailagent
summary: send stats to email 
maintainer: gedubina <gdubina@dataart.com>
description: |
  send stats to email
tags:
  # Replace "misc" with one or more whitelisted tags from this list:
  # https://juju.ubuntu.com/docs/authors-charm-metadata.html#charm-metadata
  - misc
subordinate: true
requires:
  redis:
    interface: redis
  mesos:
    interface: juju-info
    scope: container
